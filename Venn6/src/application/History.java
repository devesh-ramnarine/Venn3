package application;

public final class History {

	/*
	 * 
	 * private static History instance = null; private static Stack<Changeable>
	 * undoStack = new Stack<Changeable>();
	 * 
	 * public void execute(final Changeable cmd) {
	 * 
	 * undoStack.push(cmd); cmd.execute(); }
	 * 
	 * public void undo() { if (!undoStack.isEmpty()) { Changeable cmd =
	 * undoStack.pop(); cmd.undo(); } else { System.out.println("Nothing to undo.");
	 * }
	 * 
	 * }
	 * 
	 * public static History getInstance() { if (History.instance == null) {
	 * synchronized (History.class) { if (History.instance == null) {
	 * History.instance = new History(); } } } return History.instance; }
	 * 
	 * private History() { }
	 * 
	 */
}
